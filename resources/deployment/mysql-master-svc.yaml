apiVersion: v1
kind: Service
metadata:
  name: api-mysql-service
  namespace: kube-system
  labels:
   name: mysql-master
spec:
  type: NodePort
  ports:
  - port: 3306
    targetPort: 3306
    name: http
    nodePort: 30306
  selector:
    app: mysql-master